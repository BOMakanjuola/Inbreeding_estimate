# Inbreeding_estimate
The python code for estimating inbreeding requires only a pedigree file that is numerical and ordered numerically from 1 to n. The User should provide a parameter file named par.in and in the parameter file, the name of the pedigree file should be provided.
